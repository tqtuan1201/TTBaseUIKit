---
description: "Brief description of what this prompt does."
version: "1.0.0"
date_updated: "YYYY-MM-DD"
risk: "safe"
source: "internal"
tags: ["prompt", "workflow"]
---

# ttb-skill-{name}

Brief description of the prompt.

## When To Use

Route by semantic intent from `ttb-skill-shared/routing/intent-manifest.json`.

- English aliases: "..."
- Vietnamese aliases: "...", "..." without diacritics
- Negative signals: "..."

## Input / Output

```yaml
input:
  required: []
  optional: []
output:
  artifacts: []
  verification: "BUILD SUCCEEDED or documented blocker"
```

## Pattern

```swift
// Template code here
```

## Rules

1. Rule one
2. Rule two

## Retry / Fallback

- Retry verification at most 3 times.
- If a dependency is missing, stop and report the prerequisite.
- If route confidence is unclear, ask one focused question.

## Post-Implementation Verification (MANDATORY)

After all files are generated, **run Phase 6 verification**:

1. **Add files to Xcode project** — ensure each `.swift` is registered in `project.pbxproj`
2. **Run verification**:
   ```bash
   bash ttb-skill-shared/scripts/ttb-verify.sh
   ```
3. **Check compliance**:
   ```bash
   bash ttb-skill-shared/scripts/ttb-compliance-check.sh
   ```
4. **Skill is complete only when** `BUILD SUCCEEDED`

**Anti-Loop**: Max 3 build attempts. 3 failures — STOP, document errors.

For full FCR 7-Dimension scoring, see `ttb-skill-shared/phases/ttb-phase-verify.md`.
