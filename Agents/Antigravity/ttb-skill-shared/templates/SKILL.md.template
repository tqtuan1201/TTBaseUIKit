---
name: "ttb-skill-{name}"
description: "Brief description of what this skill does."
version: "1.0.0"
date_updated: "YYYY-MM-DD"
risk: "safe"
source: "internal"
loadLevel: "domain"  # always | domain | on-demand
tags: ["ios", "swift", "ios14", "routing"]
---

# ttb-skill-{name}

Brief description of the skill.

## Purpose

Define the exact job this skill owns and the jobs it must not own.

## When To Use

Route by semantic intent, not exact keyword only.

- English aliases: "..."
- Vietnamese aliases: "...", "..." without diacritics
- Mixed-language aliases: "..."

## Routing Contract

```yaml
input:
  required: []
  optional: []
output:
  artifacts: []
  completion_gate: "BUILD SUCCEEDED or documented blocker"
confidence:
  auto_route: ">= 0.78 when ..."
  clarify: "0.55-0.77 when ..."
fallback:
  default: "..."
```

## Mandatory Preflight Execution Gate

Before this skill generates code, refactors, migrates, modifies files, creates architecture, updates workflows, updates UI/navigation, changes dependencies, or changes business logic, run:

- `ttb-skill-shared/fragments/ttb-preflight-execution-gate.frag.md`
- `ttb-skill-shared/templates/ttb-clarification-survey.md` if confidence is below threshold

Required gate phases:

1. Requirement Analysis
2. Context Validation
3. Ambiguity Detection
4. Missing Information Detection
5. Survey / Clarification Phase
6. Confidence Evaluation
7. Execution Approval

Execution thresholds:

| Confidence | Action |
|------------|--------|
| `90-100` | Execute directly |
| `70-89` | Execute with documented low-risk assumptions |
| `<70` | Stop and ask a survey |

Cap confidence at `69` when architecture, navigation, business logic, API contract, target module, state management, localization, or ownership is unclear.

## How to Use

```bash
/ttb-{name}
```

## Commands

| Command | Description |
|---------|-------------|
| `/ttb-{name}` | Primary command |

## Pattern

```swift
// Template code here
```

## Rules

1. Rule one
2. Rule two

## Anti-Patterns

- Do not route unrelated work into this skill.
- Do not duplicate a shared phase or reference.
- Do not bypass dependency validation.

## Fallback Strategy

If context is insufficient, ask one focused clarification or route to the safer prerequisite skill.

## Shared Resources

| Resource | Purpose |
|---------|---------|
| `fragments/ttb-iron-laws.frag.md` | Mandatory laws |
| `fragments/ttb-preflight-execution-gate.frag.md` | Requirement validation and execution gate |
| `templates/ttb-clarification-survey.md` | Standard clarification surveys |
| `fragments/ttb-marker.frag.md` | File header |
| `routing/intent-router.md` | Routing contract |
| `workflows/ttb-workflow-standard.md` | State/retry/fallback workflow |

## Post-Implementation Verification (MANDATORY)

After all files are generated, **run Phase 6 verification**:

1. **Add files to Xcode project** — ensure each `.swift` is registered in `project.pbxproj`
2. **Run verification**:
   ```bash
   bash ttb-skill-shared/scripts/ttb-verify.sh
   ```
3. **Check compliance**:
   ```bash
   bash ttb-skill-shared/scripts/ttb-compliance-check.sh
   ```
4. **Skill is complete only when** `BUILD SUCCEEDED`

**Anti-Loop**: Max 3 build attempts. 3 failures — STOP, document errors.

For full FCR 7-Dimension scoring, see `ttb-skill-shared/phases/ttb-phase-verify.md`.

---

**Version**: 1.0.0 | **Date**: 2026-05-18
